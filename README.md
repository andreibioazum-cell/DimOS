# DimOS Minimal

Минимальная 16-битная система: сразу после загрузки открывается командная строка, а единственное приложение — встроенная игра «Змейка». Старые файловые, графические, звуковые и прикладные функции в образ больше не входят.

## Единственная команда

- `SNAKE` — запустить игру.

В игре используются `WASD` или стрелки. `N` начинает новую игру, `Esc` возвращает в командную строку.

## Запуск в браузере

В корне репозитория находится `index.html` — браузерный лаунчер. Он сразу, без выбора файлов, запускает встроенный образ `web/dimos.iso`: открываешь страницу — и DimOS уже грузится. Загрузить другой образ можно только в формате ISO (кнопка «Другой ISO»); образ читается локально браузером и не отправляется на сервер.

```bash
# сначала соберите ISO — он обновит и web/dimos.iso
make iso

# затем откройте веб-лаунчер
python3 -m http.server 8000
```

Перейдите на `http://localhost:8000` — DimOS запустится автоматически. Дождитесь приглашения `>`, введите `SNAKE` или нажмите зелёную экранную кнопку. Движок v86 и BIOS уже сохранены в `web/v86/`, поэтому при запуске через веб-сервер внешние CDN не требуются.

Страницу нельзя открыть двойным щелчком по файлу (`file://`) — браузер заблокирует чтение ISO. Запускайте через любой статический веб-сервер, например `python3 -m http.server`.

## Сборка

Требуются `nasm`, `dosfstools`, `mtools`, `xorriso`, `g++` и `make`.

```bash
make iso
```

Результаты:

- `disk_img/dimos.img` — загрузочная FAT12-дискета;
- `disk_img/dimos.iso` — загрузочный ISO;
- `web/dimos.iso` — копия ISO, которую автоматически запускает браузерный лаунчер.

В файловой системе образа намеренно находится только `KERNEL.BIN`: командная строка и «Змейка» встроены прямо в ядро.

## Запуск

```bash
./run-linux.sh
```

Или напрямую:

```bash
qemu-system-x86_64 -drive format=raw,file=disk_img/dimos.img,if=floppy
```

### Termux / proot-distro

```bash
apt update
apt install -y build-essential nasm dosfstools mtools xorriso qemu-system-x86 git
make iso
qemu-system-x86_64 \
  -display vnc=:0 \
  -drive format=raw,file=disk_img/dimos.img,if=floppy,index=0
```

После запуска подключитесь VNC-клиентом к `127.0.0.1:5900`.
